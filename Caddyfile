http:// {
    handle /api/* {
        reverse_proxy api:8080
    }
    handle {
        reverse_proxy web:80
    }
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
    }
}
